GOSRAN documentation diff integrity
Date: 2026-07-30

PASS: git diff --check reports no whitespace errors.
PASS: docs/artifacts/gosran-docs/artifact-contract.json parses as JSON.
PASS: GitHub issue templates parse as YAML mappings.
PASS: Governed Markdown files render to non-empty HTML.
PASS: Public-document relative links resolve.
PASS: LICENSE is unchanged.
PASS: No secret-bearing env file was read or changed.

SEPARATE WORKTREE CHANGES:
This checkout also contains pending workflow, Deployment Show authorization, helper, and regression
test changes outside the documentation lane. The documentation artifact records PHP 8.5.8 Composer,
Pint, focused Pest, npm ci, Vite build, and real local application browser results as scoped
evidence. It does not claim an actual PR or GitHub Actions run, deployment, or production proof.

KNOWN INTEGRATION BOUNDARY:
The pending .github/workflows/pr-quality.yaml change targets v4.x, passed actionlint 1.7.12 and its
branch contract, and is outside the documentation lane. All 15 workflows parse, but whole-repository
actionlint retains pre-existing SC2086 warnings in seven unrelated workflows. No actual PR or GitHub
Actions run verifies the change. CONTRIBUTING.md and the PR template remain conservative until the
parent decides how to integrate that separate lane.
